---
title: "AI Knowledge Base RAG"
excerpt: "Production-style RAG system for querying government and defense AI policy documents, built with a fully local, private stack — no OpenAI, no cloud costs."
collection: portfolio
---

This project is a production-style Retrieval-Augmented Generation (RAG) system that allows natural language querying over a corpus of government and defense AI policy documents, including NIST AI RMF, OMB M-25-21, DoD AI Strategy, and GSA AI compliance documents. The system is designed to run entirely locally with no cloud dependencies, making it well-suited for privacy-sensitive use cases common in government and defense contexts.

The pipeline ingests PDFs using PyMuPDF, splits them into overlapping chunks, embeds them using the BAAI/bge-large-en-v1.5 model via HuggingFace, and stores the resulting vectors in a Qdrant vector database. At query time, the user's question is embedded using the same model, the top-k most semantically similar chunks are retrieved, and a grounded answer with source citations is generated by Mistral 7B running locally via Ollama. The full stack is served through a FastAPI REST API and containerized with Docker Compose.

The project also includes a custom evaluation pipeline that measures retrieval recall and answer quality using an LLM-as-judge scoring approach, achieving 0.95 retrieval recall and 0.92 answer similarity across 10 ground-truth question-answer pairs. Results are visualized in a Streamlit dashboard with per-question inspection of expected vs. generated answers and source comparisons. A GitHub Actions CI/CD pipeline runs linting and unit tests on every commit.

[GitHub](https://github.com/SamZombie/AI-Knowlege-Base-RAG)

**Tech Stack:** Python, FastAPI, LangChain, Qdrant, Ollama, Mistral 7B, BAAI/bge-large-en-v1.5, PyMuPDF, Docker, Streamlit, pytest, GitHub Actions
